package cn.com.evo.cms.utils;import cn.com.evo.cms.constant.PayTypeEnum;import java.text.SimpleDateFormat;import java.util.Random;/** * @Description: * @author: lu.xin * @create: 2019-05-28 2:06 PM **/public class OrderUtils {    /**     * 生成订单号     *     * @param payType     * @return     */    public static String createOrderNo(Integer payType) {        SimpleDateFormat formatter = new SimpleDateFormat("yyyyMMddHHmmssSSS");        String dateString = formatter.format(System.currentTimeMillis());        //订单号增加6位随机数，防止重复        String r = OrderUtils.getRandom();        String start = "";        //判断支付类型        switch (PayTypeEnum.getByType(payType)) {            case ALi:                //支付宝                start = "A";                break;            case WeiXin:                //微信                start = "W";                break;            case BOSS:                //BOSS                start = "B";                break;            case SP:                //SP                start = "S";                break;            default:                break;        }        String orderNo = start + r + dateString;        return orderNo;    }    /**     * 获取随机数(6位)     *     * @return     */    public static String getRandom() {        Random r = new Random();        long num = Math.abs(r.nextLong() % 1000000L);        String s = String.valueOf(num);        for (int i = 0; i < 6 - s.length(); i++) {            s = "0" + s;        }        return s;    }    /**     * 获取随机数（8位）     *     * @return     */    public static String getRandom8() {        Random r = new Random();        long num = Math.abs(r.nextLong() % 100000000L);        String s = String.valueOf(num);        for (int i = 0; i < 6 - s.length(); i++) {            s = "0" + s;        }        return s;    }}